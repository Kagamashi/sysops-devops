
# To view the current state of the working directory and staging area:
git status

# To see changes between the working directory and the last commit:
git diff  # Show unstaged changes

# To see changes between the staging area and the last commit:
git diff --staged  # Show staged changes
