
# pkill allows killing processes based on their name instead of PID

pkill processname     # kill process by name
pkill -9 processname  # forcefully kill a process
pkill -u username     # kill all processes belonging to a user
