
# dstat combines multiple monitoring tools into a single output
sudo apt install dstat

dstat             # Show CPU, disk, and network statistics.
dstat -c -d -m    # Show CPU, disk, and memory usage.
dstat --top-cpu   # Display the most CPU-intensive processes.
